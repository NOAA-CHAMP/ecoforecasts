function maxigraph(varargin)
%CALLS: function maximize_graph(fh)
%
% Enlarge plot figure FH to fill available screen. If FH not specified,
% enlarge the GCF (see).

  maximize_graph(varargin{:});

return;
